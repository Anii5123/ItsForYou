version: '3.8'

services:
  # MongoDB Container
  mongodb:
    image: mongo:7-jammy
    container_name: foryou_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: always

  # Node.js Express Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: foryou_backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://anipatil5122003_db_user:Anii%405123@cluster0.xl5gji2.mongodb.net/foryou?retryWrites=true&w=majority&appName=Cluster0
      - JWT_SECRET=super_secret_jwt_key_foryou_2026_change_in_production
      - CLIENT_URL=http://localhost:5173
      - SMTP_HOST=smtp.gmail.com
      - SMTP_PORT=587
      - SMTP_USER=aniip5122003@gmail.com
      - SMTP_PASS=jprycrvndimtmspf
      - SMTP_FROM=For You <aniip5122003@gmail.com>
    depends_on:
      - mongodb
    restart: always

  # React Vite Frontend Service
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: foryou_frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always

volumes:
  mongo_data:
